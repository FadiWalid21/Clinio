version: '3.8'

services:
  clinio.db:
    image: mcr.microsoft.com/azure-sql-edge:latest
    container_name: clinio-sql-server
    environment:
      SA_PASSWORD: "Fadi2808"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql
      - /Users/macbookpro/dev/Docker_Volumes/SqlData/backup:/var/opt/mssql/backup

volumes:
  sqlserver-data: